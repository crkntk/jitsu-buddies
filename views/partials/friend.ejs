<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div id=<%=friend %>-list class="flex flex-row bg-carbon-black py-3 my-1 px-2 text-white" type="button">
        <div><%= friend %> </div>
        <div id=<%=friend %>-icon class="bg-gray-400 w-[12px] h-[12px] rounded-4xl m-1.5 mx-3"> 
        </div>
        <div  id=<%=friend %>-text>: Offline</div>
    </div>
</body>
<script>
    
    $('#' + "<%=friend %>-list").on("click", async function(){
        var chat = $('#chat-sect-id [id]').map(function(){
            return this.id;
        }).get();
        var friendStr = "<%=friend %>";
        console.log(chat);
        console.log(friendStr);
        console.log(chat.includes(friendStr));
        if(chat.includes(friendStr) == false){
            let chatHtml = `<div id=<%=friend %> class= "bg-white text-black w-64 mx-1 overflow-visible h-64 border-1"> 
                <h5> <%=friend %></h5>
                <div id=<%=friend %>-chat class="w-62 h-48 border-1">

                </div>
                <form method="get" class="flex flex-row w-64">
                    <div>
                    <input id=id=<%=friend %>-input class="border-1" name="submitted-name" autocomplete="name" />
                    </div>
                    <div class="justify-self-end">
                    <button class="w-16 border-2 mx-1 ">Send</button>
                    </div>
                    </form>
                </div>`;
            $('#chat-sect-id').append(chatHtml);
            
            var chatForm = document.getElementById(friendStr);
            var input = document.getElementById(friendStr+'-input');
            form.addEventListener('submit', function(e) {
    e.preventDefault();
    if (input.value) {
      socket.emit('chat message', input.value);
      input.value = '';
    }
  });
        }
    });
</script>
</html>